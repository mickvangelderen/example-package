{
  "name": "@mickvangelderen/example-package",
  "version": "0.1.0",
  "description": "A package example.",
  "main": "lib/index.js",
  "scripts": {
    "test": "npm prune && npm update && npm run clean && npm run build && node scripts/test",
    "build": "babel src --out-dir lib",
    "clean": "rm -rf lib",
    "preversion": "npm run test",
    "prepublish": "npm run build"
  },
  "keywords": [],
  "author": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/mickvangelderen/example-package/tree/master"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-core": "^6.9.0",
    "babel-preset-es2015": "^6.9.0"
  }
}
